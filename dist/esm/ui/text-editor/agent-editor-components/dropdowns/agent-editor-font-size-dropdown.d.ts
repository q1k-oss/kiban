import { Editor } from "@tiptap/core";
interface FontSizeDropdownProps {
    editor: Editor;
    isOpen: boolean;
    onToggle: () => void;
}
export declare const FontSizeDropdown: ({ editor, isOpen, onToggle, }: FontSizeDropdownProps) => import("react/jsx-runtime").JSX.Element;
export {};
