---
title: Sonner
description: A toast notification component that provides unobtrusive feedback.
tags: ["sonner", "toast", "notification", "feedback"]
---

<Links links={[
  { title: "Sonner", url: "https://sonner.emilkowal.ski/" },
]} />

### When To Use

Use the Sonner component when you need to:

- Display non-blocking notifications or feedback to users
- Show success, error, or information messages
- Notify users of system events or status changes
- Present time-sensitive information that doesn't require user action
- Provide feedback after user actions (form submissions, save operations, etc.)

Sonner notifications are temporary and disappear automatically, making them ideal for transient feedback that doesn't interrupt the user's workflow.

<Toaster />

### Examples
<Preview
  lang="jsx"
  file="/sonner/__demo__/basic"
  title="Basic"
/>

<Preview
  lang="jsx"
  file="/sonner/__demo__/types"
  title="Types"
/>

<Preview
  lang="jsx"
  file="/sonner/__demo__/custom"
  title="Custom"
/>

<Preview
  lang="jsx"
  file="/sonner/__demo__/promise"
  title="Promise"
/>

### Props
#### ExternalToast
<AutoTypeTable type={`
  import { type ExternalToast } from "sonner";
  export type ExternalToast = ExternalToast
`}
  name="ExternalToast"
/> 
#### ToastClassnames
<AutoTypeTable type={`
  import { type ToastClassnames } from "sonner";
  export type ToastClassnames = ToastClassnames
`}
  name="ToastClassnames"
/> 
#### ToastT
<AutoTypeTable type={`
  import { type ToastT } from "sonner";
  export type ToastT = ToastT
`}
  name="ToastT"
/> 
#### ToastToDismiss
<AutoTypeTable type={`
  import { type ToastToDismiss } from "sonner";
  export type ToastToDismiss = ToastToDismiss
`}
  name="ToastToDismiss"
/> 