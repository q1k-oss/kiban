---
title: Switch
description: A control that allows the user to toggle between checked and unchecked states.
tags: ["switch", "toggle", "input", "control"]
---

<Links links={[
  { title: "Radix UI Switch", url: "https://www.radix-ui.com/primitives/docs/components/switch" },
]} />

### When To Use

Use the Switch component when you need to:

- Allow users to toggle a single option on or off
- Provide a binary choice with immediate effect
- Create an on/off control for settings or features
- Enable or disable a functionality
- Create a visual toggle that clearly shows the current state

Switches are ideal for settings pages, feature toggles, and anywhere you need to show a clear binary state with immediate visual feedback.

### Examples
<Preview
  lang="jsx"
  file="/switch/__demo__/basic"
  title="Basic"
/>

<Preview
  lang="jsx"
  file="/switch/__demo__/with-label"
  title="With Label"
/>

<Preview
  lang="jsx"
  file="/switch/__demo__/disabled"
  title="Disabled"
/>

<Preview
  lang="jsx"
  file="/switch/__demo__/custom"
  title="Custom"
/>

### Props
<AutoTypeTable type={`
  import { type SwitchProps } from "@radix-ui/react-switch";
  export type SwitchProps = Omit<SwitchProps, keyof React.HTMLAttributes<HTMLSpanElement>>
`}
  name="SwitchProps"
/> 
#### SwitchThumb
<AutoTypeTable type={`
  import { type SwitchThumbProps } from "@radix-ui/react-switch";
  export type SwitchThumbProps = Omit<SwitchThumbProps, keyof React.HTMLAttributes<HTMLSpanElement>>
`}
  name="SwitchThumbProps"
/> 