---
title: Tabs
description: A set of layered sections of content, known as tab panels, that display one panel of content at a time.
tags: ["tabs", "tab", "panel", "content"]
---

<Links links={[
  { title: "Radix UI Tabs", url: "https://www.radix-ui.com/primitives/docs/components/tabs" },
]} />

### When To Use

Use the Tabs component when you need to:

- Organize related content into separate views where only one view is visible at a time
- Allow users to switch between different sections without navigating to a new page
- Save space by showing only the relevant content for the selected tab
- Group content by category or function
- Create a workflow where users need to focus on one section at a time

Tabs are especially useful for forms, settings pages, dashboards, and any interface where multiple related sections of content need to be organized.

### Examples
<Preview
  lang="jsx"
  file="/tabs/__demo__/basic"
  title="Basic"
/>

<Preview
  lang="jsx"
  file="/tabs/__demo__/custom"
  title="Custom"
/>

<Preview
  lang="jsx"
  file="/tabs/__demo__/vertical"
  title="Vertical"
/>

### Props
#### Tabs
<AutoTypeTable type={`
  import { type TabsProps } from "@radix-ui/react-tabs";
  export type TabsProps = Omit<TabsProps, keyof React.HTMLAttributes<HTMLDivElement>>
`}
  name="TabsProps"
/>
#### TabsList
<AutoTypeTable type={`
  import { type TabsListProps } from "@radix-ui/react-tabs";
  export type TabsListProps = Omit<TabsListProps, keyof React.HTMLAttributes<HTMLDivElement>>
`}
  name="TabsListProps"
/>
#### TabsTrigger
<AutoTypeTable type={`
  import { type TabsTriggerProps } from "@radix-ui/react-tabs";
  export type TabsTriggerProps = Omit<TabsTriggerProps, keyof React.HTMLAttributes<HTMLButtonElement>>
`}
  name="TabsTriggerProps"
/>
#### TabsContent
<AutoTypeTable type={`
  import { type TabsContentProps } from "@radix-ui/react-tabs";
  export type TabsContentProps = Omit<TabsContentProps, keyof React.HTMLAttributes<HTMLDivElement>>
`}
  name="TabsContentProps"
/>